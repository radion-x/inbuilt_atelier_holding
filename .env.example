# Server configuration
PORT=3000
PAYLOAD_LIMIT=1mb

# Mailgun configuration (REST API)
MAILGUN_API_KEY=your-mailgun-private-key
MAILGUN_DOMAIN=mg.yourdomain.com
MAILGUN_TO=info@yourdomain.com
MAILGUN_FROM=Confidental Cosmetic Dentistry <no-reply@yourdomain.com>
MAILGUN_USERNAME=api # Optional override; Mailgun defaults to "api"
# MAILGUN_PASSWORD= # Optional fallback if you want to supply the key via MAILGUN_PASSWORD instead
# MAILGUN_API_BASE=https://api.mailgun.net # Override for EU regions
